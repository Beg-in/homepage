cmain
  .hero
    .frow-container.height-100
      .frow
        h2
          | Meet the Team
  .people
    .frow
      .col-sm-1-2.col-md-1-3(v-for="member in team")
        .person
          .frow.column-center
            .card-title
              | {{`${member.firstName} ${member.lastName}`}}
            img(:src="member.img")
            .card-description
              | {{member.title}}
              <br>
              | {{member.description}}